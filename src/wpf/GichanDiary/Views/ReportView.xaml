<UserControl x:Class="GichanDiary.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:GichanDiary.ViewModels"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:ReportViewModel, IsDesignTimeCreatable=False}">

    <DockPanel>
        <!-- ═══ Period Filter Bar (sticky at top) ═══ -->
        <Border DockPanel.Dock="Top" Style="{DynamicResource CardBorder}" Margin="16,12,16,0">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Path Data="{DynamicResource IconReport}" Stroke="{DynamicResource CatSystemBrush}" StrokeThickness="1.5" Width="20" Height="20" Stretch="Uniform" Margin="0,0,8,0"/>
                        <TextBlock Text="분석 기간" FontSize="21" FontWeight="Bold" Foreground="{DynamicResource CatSystemBrush}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <StackPanel.Resources>
                            <!-- 기본 기간 버튼: SecondaryButton 베이스, 선택 시 CatSystemBrush 배경 + 흰 글자 -->
                            <Style x:Key="PeriodBase" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
                                <Setter Property="FontSize" Value="19"/>
                                <Setter Property="Margin" Value="4,0"/>
                                <Setter Property="Padding" Value="16,8"/>
                            </Style>
                        </StackPanel.Resources>
                        <Button Content="1일" Command="{Binding SetPeriodCommand}" CommandParameter="1일">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PeriodBase}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedPeriod}" Value="1일">
                                            <Setter Property="Background" Value="{DynamicResource CatSystemBrush}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="3일" Command="{Binding SetPeriodCommand}" CommandParameter="3일">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PeriodBase}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedPeriod}" Value="3일">
                                            <Setter Property="Background" Value="{DynamicResource CatSystemBrush}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="7일" Command="{Binding SetPeriodCommand}" CommandParameter="7일">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PeriodBase}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedPeriod}" Value="7일">
                                            <Setter Property="Background" Value="{DynamicResource CatSystemBrush}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="14일" Command="{Binding SetPeriodCommand}" CommandParameter="14일">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PeriodBase}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedPeriod}" Value="14일">
                                            <Setter Property="Background" Value="{DynamicResource CatSystemBrush}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="30일" Command="{Binding SetPeriodCommand}" CommandParameter="30일">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PeriodBase}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedPeriod}" Value="30일">
                                            <Setter Property="Background" Value="{DynamicResource CatSystemBrush}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button Content="전체" Command="{Binding SetPeriodCommand}" CommandParameter="전체">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PeriodBase}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedPeriod}" Value="전체">
                                            <Setter Property="Background" Value="{DynamicResource CatSystemBrush}"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </StackPanel>
        </Border>

        <!-- ═══ Scrollable charts area ═══ -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16,12">
            <StackPanel Margin="16,0,16,12">

            <!-- ═══ Today Summary (6카드 1행, 제목:컨텐츠 = 1:2 비율) ═══ -->
            <UniformGrid Columns="6" Margin="0,0,0,12">
                <!-- 오늘 수유 횟수 -->
                <Border Background="{DynamicResource BgCardBrush}" CornerRadius="14"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        Padding="8,6" Margin="0,0,3,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="2*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="오늘 수유 횟수" FontSize="14" Foreground="{DynamicResource TextMidBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Grid.Row="1" Text="{Binding TodayFeedCount}" FontSize="24" FontWeight="Bold"
                                   Foreground="{DynamicResource CatFeedBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
                <!-- 오늘 수유량 -->
                <Border Background="{DynamicResource BgCardBrush}" CornerRadius="14"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        Padding="8,6" Margin="2,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="2*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="오늘 수유량" FontSize="14" Foreground="{DynamicResource TextMidBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Grid.Row="1" Text="{Binding TodayFeedTotal}" FontSize="24" FontWeight="Bold"
                                   Foreground="{DynamicResource CatFeedBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
                <!-- 오늘 배변: 소변/대변 각각 구분색 -->
                <Border Background="{DynamicResource BgCardBrush}" CornerRadius="14"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        Padding="8,6" Margin="2,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="2*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="오늘 배변" FontSize="14" Foreground="{DynamicResource TextMidBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock FontSize="24" FontWeight="Bold" Foreground="{DynamicResource CatUrineBrush}">
                                <Run Text="소"/>
                                <Run Text="{Binding TodayUrineCount}"/>
                            </TextBlock>
                            <TextBlock Text=" / " FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextMidBrush}"/>
                            <TextBlock FontSize="24" FontWeight="Bold" Foreground="{DynamicResource CatStoolBrush}">
                                <Run Text="대"/>
                                <Run Text="{Binding TodayStoolCount}"/>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </Border>
                <!-- 일평균 수유량 -->
                <Border Background="{DynamicResource BgCardBrush}" CornerRadius="14"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        Padding="8,6" Margin="2,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="2*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="일평균 수유량" FontSize="14" Foreground="{DynamicResource TextMidBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Grid.Row="1" Text="{Binding AvgFeedAmount}" FontSize="24" FontWeight="Bold"
                                   Foreground="{DynamicResource CatFeedBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
                <!-- 평균 수유텀 -->
                <Border Background="{DynamicResource BgCardBrush}" CornerRadius="14"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        Padding="8,6" Margin="2,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="2*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="평균 수유텀" FontSize="14" Foreground="{DynamicResource TextMidBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBlock Grid.Row="1" Text="{Binding AvgFeedInterval}" FontSize="24" FontWeight="Bold"
                                   Foreground="{DynamicResource CatFeedBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
                <!-- 신체: 키/몸무게 2열 좌측정렬, 전체 중앙배치 -->
                <Border Background="{DynamicResource BgCardBrush}" CornerRadius="14"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        Padding="8,6" Margin="3,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="2*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="신체" FontSize="14" Foreground="{DynamicResource TextMidBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="키" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource CatBodyBrush}" Margin="0,0,8,0"/>
                            <TextBlock Grid.Column="1" Text="{Binding LastHeight}" FontSize="18" FontWeight="Bold"
                                       Foreground="{DynamicResource CatBodyBrush}"/>
                            <TextBlock Grid.Row="1" Text="몸무게" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource CatBodyBrush}" Margin="0,0,8,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding LastWeight}" FontSize="18" FontWeight="Bold"
                                       Foreground="{DynamicResource CatBodyBrush}"/>
                        </Grid>
                    </Grid>
                </Border>
            </UniformGrid>

            <!-- ═══ 1회 수유량 변화 추이 (최상단) ═══ -->
            <Border Style="{DynamicResource CardBorder}" Margin="0,0,0,12">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Path Data="{DynamicResource IconReport}" Stroke="{DynamicResource CatFeedBrush}" StrokeThickness="1.5" Width="20" Height="20" Stretch="Uniform" Margin="0,0,8,0"/>
                        <TextBlock Text="1회 수유량 변화 추이" FontSize="21" FontWeight="Bold" Foreground="{DynamicResource CatFeedBrush}"/>
                    </StackPanel>
                    <lvc:CartesianChart Series="{Binding FeedAmountSeries}"
                                         XAxes="{Binding FeedAmountXAxes}"
                                         YAxes="{Binding FeedAmountYAxes}"
                                         Height="250"
                                         Background="Transparent"
                                         TooltipBackgroundPaint="{Binding TooltipBgPaint}"
                                         TooltipTextPaint="{Binding TooltipTextPaint}"
                                         TooltipPosition="Top"/>
                </StackPanel>
            </Border>

            <!-- ═══ 일별 수유량 추이 ═══ -->
            <Border Style="{DynamicResource CardBorder}" Margin="0,0,0,12">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Path Data="{DynamicResource IconReport}" Stroke="{DynamicResource CatFeedBrush}" StrokeThickness="1.5" Width="20" Height="20" Stretch="Uniform" Margin="0,0,8,0"/>
                        <TextBlock Text="일별 수유량 추이" FontSize="21" FontWeight="Bold" Foreground="{DynamicResource CatFeedBrush}"/>
                    </StackPanel>
                    <lvc:CartesianChart Series="{Binding DailyFeedSeries}"
                                         XAxes="{Binding DailyFeedXAxes}"
                                         YAxes="{Binding DailyFeedYAxes}"
                                         Height="250"
                                         Background="Transparent"
                                         TooltipBackgroundPaint="{Binding TooltipBgPaint}"
                                         TooltipTextPaint="{Binding TooltipTextPaint}"
                                         TooltipPosition="Top"/>
                </StackPanel>
            </Border>

            <!-- ═══ 최근 수유텀 추이 (신규) ═══ -->
            <Border Style="{DynamicResource CardBorder}" Margin="0,0,0,12">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Path Data="{DynamicResource IconReport}" Stroke="{DynamicResource CatFeedBrush}" StrokeThickness="1.5" Width="20" Height="20" Stretch="Uniform" Margin="0,0,8,0"/>
                        <TextBlock Text="최근 수유텀 추이" FontSize="21" FontWeight="Bold" Foreground="{DynamicResource CatFeedBrush}"/>
                    </StackPanel>
                    <lvc:CartesianChart Series="{Binding FeedIntervalTrendSeries}"
                                         XAxes="{Binding FeedIntervalTrendXAxes}"
                                         YAxes="{Binding FeedIntervalTrendYAxes}"
                                         Height="250"
                                         Background="Transparent"
                                         TooltipBackgroundPaint="{Binding TooltipBgPaint}"
                                         TooltipTextPaint="{Binding TooltipTextPaint}"
                                         TooltipPosition="Top"/>
                </StackPanel>
            </Border>

            <!-- ═══ 수유텀 분포 + 일별 배변 횟수 (나란히) ═══ -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{DynamicResource CardBorder}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Path Data="{DynamicResource IconReport}" Stroke="{DynamicResource CatSystemBrush}" StrokeThickness="1.5" Width="20" Height="20" Stretch="Uniform" Margin="0,0,8,0"/>
                            <TextBlock Text="수유텀 분포" FontSize="21" FontWeight="Bold" Foreground="{DynamicResource CatSystemBrush}"/>
                        </StackPanel>
                        <lvc:CartesianChart Series="{Binding FeedIntervalSeries}"
                                             XAxes="{Binding FeedIntervalXAxes}"
                                             YAxes="{Binding FeedIntervalYAxes}"
                                             Height="250"
                                             Background="Transparent"
                                             TooltipBackgroundPaint="{Binding TooltipBgPaint}"
                                             TooltipTextPaint="{Binding TooltipTextPaint}"
                                             TooltipPosition="Top"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{DynamicResource CardBorder}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Path Data="{DynamicResource IconReport}" Stroke="{DynamicResource CatBowelBrush}" StrokeThickness="1.5" Width="20" Height="20" Stretch="Uniform" Margin="0,0,8,0"/>
                            <TextBlock Text="일별 배변 횟수" FontSize="21" FontWeight="Bold" Foreground="{DynamicResource CatBowelBrush}"/>
                        </StackPanel>
                        <lvc:CartesianChart Series="{Binding DailyBowelSeries}"
                                             XAxes="{Binding DailyBowelXAxes}"
                                             YAxes="{Binding DailyBowelYAxes}"
                                             Height="250"
                                             Background="Transparent"
                                             LegendPosition="Bottom"
                                             LegendTextPaint="{Binding LegendTextPaint}"
                                             TooltipBackgroundPaint="{Binding TooltipBgPaint}"
                                             TooltipTextPaint="{Binding TooltipTextPaint}"
                                             TooltipPosition="Top"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ═══ 키 변화량 + 몸무게 변화량 (나란히) ═══ -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{DynamicResource CardBorder}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Path Data="{DynamicResource IconReport}" Stroke="{DynamicResource CatBodyBrush}" StrokeThickness="1.5" Width="20" Height="20" Stretch="Uniform" Margin="0,0,8,0"/>
                            <TextBlock Text="키 변화량" FontSize="21" FontWeight="Bold" Foreground="{DynamicResource CatBodyBrush}"/>
                        </StackPanel>
                        <lvc:CartesianChart Series="{Binding HeightSeries}"
                                             XAxes="{Binding HeightXAxes}"
                                             YAxes="{Binding HeightYAxes}"
                                             Height="250"
                                             Background="Transparent"
                                             TooltipBackgroundPaint="{Binding TooltipBgPaint}"
                                             TooltipTextPaint="{Binding TooltipTextPaint}"
                                             TooltipPosition="Top"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{DynamicResource CardBorder}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Path Data="{DynamicResource IconReport}" Stroke="{DynamicResource CatBodyBrush}" StrokeThickness="1.5" Width="20" Height="20" Stretch="Uniform" Margin="0,0,8,0"/>
                            <TextBlock Text="몸무게 변화량" FontSize="21" FontWeight="Bold" Foreground="{DynamicResource CatBodyBrush}"/>
                        </StackPanel>
                        <lvc:CartesianChart Series="{Binding WeightSeries}"
                                             XAxes="{Binding WeightXAxes}"
                                             YAxes="{Binding WeightYAxes}"
                                             Height="250"
                                             Background="Transparent"
                                             TooltipBackgroundPaint="{Binding TooltipBgPaint}"
                                             TooltipTextPaint="{Binding TooltipTextPaint}"
                                             TooltipPosition="Top"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ═══ 카테고리별 이벤트 비율 + 일별 카테고리별 이벤트 (나란히) ═══ -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{DynamicResource CardBorder}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Path Data="{DynamicResource IconReport}" Stroke="{DynamicResource CatSystemBrush}" StrokeThickness="1.5" Width="20" Height="20" Stretch="Uniform" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding CategoryPieTitle}" FontSize="21" FontWeight="Bold" Foreground="{DynamicResource CatSystemBrush}"/>
                        </StackPanel>
                        <lvc:PieChart Series="{Binding CategoryPieSeries}"
                                       Height="220"
                                       Background="Transparent"
                                       LegendPosition="Bottom"
                                       LegendTextPaint="{Binding LegendTextPaint}"
                                       TooltipBackgroundPaint="{Binding TooltipBgPaint}"
                                       TooltipTextPaint="{Binding TooltipTextPaint}"
                                       TooltipPosition="Top"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{DynamicResource CardBorder}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <Path Data="{DynamicResource IconReport}" Stroke="{DynamicResource CatSystemBrush}" StrokeThickness="1.5" Width="20" Height="20" Stretch="Uniform" Margin="0,0,8,0"/>
                            <TextBlock Text="일별 이벤트 현황" FontSize="21" FontWeight="Bold" Foreground="{DynamicResource CatSystemBrush}"/>
                        </StackPanel>
                        <lvc:CartesianChart Series="{Binding DailyCategorySeries}"
                                             XAxes="{Binding DailyCategoryXAxes}"
                                             YAxes="{Binding DailyCategoryYAxes}"
                                             Height="220"
                                             Background="Transparent"
                                             LegendPosition="Bottom"
                                             LegendTextPaint="{Binding LegendTextPaint}"
                                             TooltipBackgroundPaint="{Binding TooltipBgPaint}"
                                             TooltipTextPaint="{Binding TooltipTextPaint}"
                                             TooltipPosition="Top"/>
                    </StackPanel>
                </Border>
            </Grid>

            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
